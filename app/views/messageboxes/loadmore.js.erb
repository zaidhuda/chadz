<% @messages.each do |message| %>
$('#messages-container').append("<%= escape_javascript(render :partial => 'messages/messages', :locals => { :message => message }) %>");
$('#next_page').html("<%= escape_javascript(link_to_next_page @messages, 'Load More', :remote=>true, :id => 'next_page') %>");
<% end %>
removeDuplicateID();